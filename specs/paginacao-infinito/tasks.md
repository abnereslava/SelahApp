# Tasks: Paginação com Scroll Infinito

- [x] registros.js: adicionar `limit, startAfter` no import; reescrever `fetchAll` com `fetchPage(isFirst)`, `initSentinelObserver`; adicionar `#devotionalsSentinel` ao HTML; `renderFeed(arr, replace=true)` com append mode
- [x] bencaos.js: idem para bênçãos com `#blessingsSentinel`
- [x] CSS: `.load-more-sentinel`, `.list-end-msg`
