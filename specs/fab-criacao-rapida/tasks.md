# Tasks: FAB de Criação Rápida

- [x] CSS: `.nav-fab`, `.fab-sheet`, `.fab-sheet-overlay`, `.desktop-fab`, `.create-overlay`, `.stats-card`
- [x] index.html: FAB no centro do bottom nav, bottom sheet HTML, desktop FAB button
- [x] script.js: `openFabSheet()`, `closeFabSheet()`, `window.openCreateOverlay(type)`, check `_pendingCreateOverlay` em `handleRouteChange`
- [x] registros.js: stats card no topo, create overlay com form, `window.openCreateRegistrosOverlay()`, `window.closeCreateRegistrosOverlay()`, `editRecord` abre overlay
- [x] bencaos.js: idem para bênçãos
